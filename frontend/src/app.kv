#:import dp kivy.metrics.dp
#:import LoginScreen screens.login.logic.LoginScreen
#:import SignupScreen screens.signup.logic.SignupScreen
#:import HomeScreen screens.home.logic.HomeScreen
#:import PostsScreen screens.posts.logic.PostsScreen
#:import ForgotPassword screens.forgot_password.logic.ForgotPassword
#:import CreatePost screens.create_post.logic.CreatePost
#:import CreateGroup screens.create_group.logic.CreateGroup
#:import EmailCode screens.email_code.logic.EmailCode
#:import Comments screens.comments.logic.Comments

MDNavigationLayout:

    ScreenManager:
        id: screen_manager

        HomeScreen:
            name: "home" 

        Comments:
            name: "comments" 

        LoginScreen:
            name: "login"

        SignupScreen:
            name: "signup"

        PostsScreen:
            name: "posts"

        ForgotPassword:
            name: "forgot_password"

        CreatePost:
            name: "create_post"

        EmailCode:
            name: "email_code"

        CreateGroup:
            name: "create_group"

    MDNavigationDrawer:
        id: nav_drawer
        radius: 0, dp(16), dp(16), 0

        MDNavigationDrawerMenu:
            id: nav_drawer_menu